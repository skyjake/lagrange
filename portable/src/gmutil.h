#pragma once
#include <lagrange/gmutil.h>

/* Shell-only extensions. */
const iBlock *  urlPaletteSeed_String   (const iString *); /* uses sitespec */
const iString * prettyDataUrl_String    (const iString *, int contentColor); /* uses color */
